# CV Pipeline Dependencies
# Install on edge device (Raspberry Pi 4 / Jetson Nano)
#
# Running with `--simulate` (no camera hardware) only needs `requests` -
# opencv-python, numpy, and ultralytics are for real detection only:
#   pip install requests
opencv-python>=4.8.0
numpy>=1.24.0
requests>=2.31.0
ultralytics>=8.2.0

# Optional: For GPU acceleration on Jetson
# tensorrt>=8.6.0
# onnxruntime>=1.15.0
